{
  "name": "swe_site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --outfile=dist/bundle.js --format=esm --jsx=automatic --loader:.js=jsx",
    "watch": "esbuild index.js --bundle --outfile=dist/bundle.js --format=esm --jsx=automatic --watch --loader:.js=jsx"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rweir4/portfolio_site.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rweir4/portfolio_site/issues"
  },
  "homepage": "https://github.com/rweir4/portfolio_site#readme",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@babel/preset-react": "^7.27.1",
    "esbuild": "^0.25.9"
  }
}
